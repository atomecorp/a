# Full Bevy Renderer Remaining Work — Active Execution Task

Active tracker for the remaining work moved out of `todo/full_bevy_renderer.md`.

Scope:
- finish the P0 cleanup debt discovered during the Bevy/WebGPU renderer cleanup;
- keep the original full Bevy renderer tracker free of active duplicated tasks;
- keep M7.1/M7.2 here as gated validation tasks until the NewMolecules reintegration makes their editor scenarios executable.

Progress rule:
- Total steps: 42.
- Progress = `floor(done_steps / 42 * 100)`.
- Mark a step `[x]` only after validation evidence is recorded.

## Execution Steps

- [x] R0 Move remaining work out of `todo/full_bevy_renderer.md` and make this file the single active tracker for remaining Bevy-renderer cleanup. Evidence: remaining P0 and M7.1/M7.2 tasks moved here; old tracker active open/partial checklist removed. Progress: 2% complete, 41/42 steps remaining.
- [x] R1 Reduce and untangle `eVe/intuition/eVeIntuition.js` below the applicable threshold or document a validated split plan with extracted owners. Evidence: documented the validated split plan in `maps/CODEMAP.md` and moved three real eVeIntuition owners out of the legacy point of coordination without adding fallback routes: `main_menu_auth_runtime.js` owns authenticated/disconnected main-menu patching and initial login sequencing, `main_tool_registration_runtime.js` owns base main-tool Atome registration and duplicate guardrails, and `tool_window_bridge_runtime.js` owns the closed `window.__eveTextTool`/`window.__eveFinderUiRuntime` bridge; updated `maps/API_MAP.md` for the closed window bridge boundary; reduced `eVeIntuition.js` 8331->8096 lines, and the three extracted modules are below 500 lines (107/103/151); targeted `node --check` PASS for `eVeIntuition.js` and the three extracted modules; `npm run check:syntax` PASS 733 files. Progress: 90% complete, 4/42 steps remaining.
- [x] R2 Reduce and untangle `eVe/intuition/tools/core/tool_runtime.js` along real tool-runtime ownership boundaries. Evidence: kept `tool_runtime.js` as the V2 tool-runtime bootstrap/invocation coordinator and extracted focused owners into `tool_runtime_registered_handler.js` (registered-handler publication, handler latch state, window-function calls), `tool_runtime_atome_ops.js` (bootstrap Atome modifiability gates, sanitized set batches, duplicate/opacity/z-index/align/distribute/move-relative operations), `tool_runtime_creator_media.js` (creator media URL normalization and intrinsic image/video sizing probes), and `tool_runtime_context.js` (selection/current-project/active-target runtime context); removed dead imports created by the split; updated CODEMAP/API_MAP; reduced `tool_runtime.js` 6088->5252 lines and all four extracted modules are below 500 lines; targeted `node --check` PASS for the coordinator and four extracted modules; `npm run check:syntax` PASS 733 files. Progress: 92% complete, 3/42 steps remaining.
- [x] R3 Reduce and untangle `eVe/intuition/tools/project_drop.js` along project-drop/projection ownership boundaries. Evidence: kept `project_drop.js` as the project drop/projection toolbox coordinator and extracted focused owners into `project_drop_external_runtime.js` (native/external file import through canonical `ui.creator`, upload result normalization, stack-position merge, creator invocation) and `project_drop_dedupe_runtime.js` (Finder/tool drop token de-duplication and in-flight token state); preserved the existing `importFilesToProjectViaCreator` export through the injected runtime; updated CODEMAP/API_MAP; reduced `project_drop.js` 7444->7201 lines and both extracted modules are below 500 lines; targeted `node --check` PASS for the coordinator and two extracted modules; `npm run check:syntax` PASS 733 files. Progress: 95% complete, 2/42 steps remaining.
- [x] R4 Reduce and untangle `eVe/intuition/menu/core/toolbox_runtime.js` along toolbox runtime ownership boundaries. Evidence: kept `toolbox_runtime.js` as the toolbox DOM/runtime coordinator and extracted focused owners into `toolbox_runtime_model.js` (entry/model normalization, slider defaults, drag payload serialization, active drag cache, exclusive groups, pointer/number helpers, sizing constants), `toolbox_latch_runtime.js` (latched-state mutation, alias application, exclusive groups, latch epochs, `eve:tool-state-changed` emission), `toolbox_hold_runtime.js` (long-press timers/click suppression), `toolbox_slider_runtime.js` (slider input/change dispatch and debounced apply), `toolbox_drag_runtime.js` (drag binding, ghost preview, active drag payload, drop-session hooks), `toolbox_external_open_runtime.js` (external-open target resolution and reveal width), `toolbox_handle_runtime.js` (main handle click/swipe), `toolbox_palette_runtime.js` (expanded palette visual state and scroll reveal), `toolbox_entry_runtime.js` (entry invocation, latch rollback/update, momentary pulse), and `toolbox_state_sync_runtime.js` (tool-state sync and public latch adapters); removed dead `clearActiveToolDragPayload`; updated CODEMAP/DESIGN_MAP; reduced `toolbox_runtime.js` 1483->499 lines and all extracted toolbox modules are below 500 lines; `node --check` PASS for the toolbox coordinator and ten extracted modules; `npm run check:syntax` PASS 733 files. Progress: 86% complete, 6/42 steps remaining.
- [x] R5 Reduce and untangle `eVe/intuition/ribbon/menu.js` along ribbon/menu ownership boundaries. Evidence: kept `menu.js` as the ribbon bootstrap/coordinator and extracted focused owners into `menu_shell_runtime.js` (layer/root/handle/viewport/cap shell creation), `menu_delete_visual_runtime.js` (delete target/backdrop visuals, dragged-node promotion/attraction, delete API bridge), `menu_drag_delete_runtime.js` (drag-delete event routing and native drop claiming), `menu_layout_runtime.js` (metrics, overflow bounds, reveal stops, layout sync), `menu_scroll_runtime.js` (wheel/native scroll snap and cap scroll stepping), `menu_tool_render_runtime.js` (tool/palette DOM rendering, slider mounting, drag payload setup, press-hover routing), `menu_visibility_runtime.js` (open/close/dismiss/show/hide state), `menu_placement_runtime.js` (placement and handedness reflow), `menu_public_api_runtime.js` (public facade and latch adapters), plus prior `menu_model.js`, `menu_auth_dock_runtime.js`, `menu_external_open_runtime.js`, and `menu_quick_capture_runtime.js`; removed dead debug/no-op delete tracing; updated CODEMAP/DESIGN_MAP; reduced `menu.js` 4033->490 lines and extracted ribbon modules are below 500 lines; targeted `node --check` PASS for the coordinator and extracted modules; `npm run check:syntax` PASS 733 files. Progress: 88% complete, 5/42 steps remaining.
- [x] R6 Reduce `atome/renderers/bevy-core/src/types.rs` without splitting DTO ownership artificially. Evidence: split disposable Bevy ECS components/resources into `atome/renderers/bevy-core/src/components.rs` while keeping render DTOs/ops in `types.rs`; updated maps for the new owner boundary; reduced `types.rs` 527->405 lines; `cargo check --manifest-path atome/renderers/bevy-core/Cargo.toml` PASS; `cargo test --manifest-path atome/renderers/bevy-core/Cargo.toml` PASS 19/19. Progress: 7% complete, 39/42 steps remaining.
- [x] R7 Reduce `atome/renderers/bevy-core/src/video_external_texture.rs` along Bevy video external-texture responsibility boundaries. Evidence: moved focused Rust guardrails into `atome/renderers/bevy-core/src/video_external_texture_tests.rs`; kept production external-texture component/plugin/mesh ownership in `video_external_texture.rs`; updated code map; reduced `video_external_texture.rs` 690->123 lines; `cargo check --manifest-path atome/renderers/bevy-core/Cargo.toml` PASS; `cargo test --manifest-path atome/renderers/bevy-core/Cargo.toml` PASS 19/19. Progress: 9% complete, 38/42 steps remaining.
- [x] R8 Reduce `atome/src/squirrel/ai/default_tools.js` without duplicating AI tool registry ownership. Evidence: kept `default_tools.js` as the single coordinator for `AtomeAI.registerTool`; extracted domain registrations into `default_tools_adole.js`, `default_tools_calendar.js`, `default_tools_mail.js`, `default_tools_contacts.js`, `default_tools_bank.js`, and `default_tools_share.js`; updated maps; reduced `default_tools.js` 1361->200 lines, extracted modules all below 500 lines; `node --check` PASS for coordinator and six modules; `node --test atome/src/squirrel/ai/default_tools.runtime_bridge.test.mjs` PASS after adding an explicit March 2026 bank reference to remove date drift; `node --test atome/src/squirrel/ai/default_tools.runtime_trace_integration.test.mjs` PASS; `npm run check:syntax` PASS 680 files. Progress: 11% complete, 37/42 steps remaining.
- [x] R9 Reduce `atome/src/squirrel/atome/mcp.js` along MCP capability and ACL ownership boundaries. Evidence: kept `mcp.js` as the public JSON-RPC sync/async handler and global installation surface; extracted protocol/event/operation state to `mcp_core.js`, actor/security journal/proposal/confirmation/idempotency state to `mcp_security.js`, ACL/resource/prompt capability policy and rate-limit rule definitions to `mcp_security_policy.js`, platform API resolvers to `mcp_bridges.js`, communication list/search/read helpers to `mcp_communication.js`, unified tool discovery to `mcp_discovery.js`, resources/prompts to `mcp_resources.js`, runtime invocation normalization to `mcp_runtime.js`, and handler composition into `mcp_handlers.js`, `mcp_handlers_platform.js`, `mcp_handlers_ai_runtime.js`, and `mcp_handlers_communication.js`; removed dead `extractAtomePayload`; updated CODEMAP/API_MAP; reduced `mcp.js` 2560->230 lines and all MCP modules are below 500 lines; file-by-file `node --check` PASS for the facade and extracted modules; `node --test atome/src/squirrel/atome/mcp.security_surface.test.mjs atome/src/squirrel/atome/mcp.platform_surface.test.mjs atome/src/squirrel/atome/mcp.communication_surface.test.mjs atome/src/squirrel/atome/mcp.runtime_bridge.test.mjs` PASS 4/4; `npm run check:syntax` PASS 733 files. Progress: 54% complete, 19/42 steps remaining.
- [x] R10 Reduce `atome/src/squirrel/voice/home_surface.js` along voice home-surface ownership boundaries. Evidence: completed the partial extraction by removing duplicated inline transcript/i18n helpers that made `home_surface.js` fail `node --check`; kept `home_surface.js` as the public `mountHomeVoiceSurface` orchestration entry; split timing/history/state helpers into `home_surface_state.js`, disposable DOM composition/render refresh into `home_surface_view.js`, assistant response extraction into `home_surface_response.js`, transcript timers into `home_surface_transcript_runtime.js`, explicit interrupt handling into `home_surface_interrupt_runtime.js`, turn/session/follow-up execution into `home_surface_turn_runtime.js`, listening subscription/loop ownership into `home_surface_listening_runtime.js`, and meter lifecycle into `home_surface_meter_runtime.js`; preserved existing `home_surface_i18n.js` and `home_surface_transcript.js` as canonical helper owners; reduced `home_surface.js` 1772->203 lines and all home-surface modules are below 500 lines; fixed the no-user French ready line regression and explicit interrupt restart timing discovered by focused tests; aligned `home_surface.fragment_buffer.test.mjs` with the existing JSDOM canvas fixture pattern to remove non-fatal canvas errors; updated CODEMAP/API_MAP; `node --check` PASS for all home-surface modules and touched test; `node --test atome/src/squirrel/voice/home_surface.test.mjs atome/src/squirrel/voice/home_surface.fragment_buffer.test.mjs atome/src/squirrel/voice/home_surface.interrupt_command.test.mjs atome/src/squirrel/voice/home_surface.voice_availability.test.mjs atome/src/squirrel/voice/home_surface.followup_decision.test.mjs atome/src/squirrel/voice/home_surface.locale_history.test.mjs atome/src/squirrel/voice/home_surface.text_only.test.mjs atome/src/squirrel/voice/home_surface.barge_in.test.mjs atome/src/squirrel/voice/home_surface.pause_commit.test.mjs atome/src/squirrel/voice/home_surface.voice_meter.test.mjs` PASS 10/10 with clean output. Progress: 47% complete, 22/42 steps remaining.
- [x] R11 Reduce `atome/src/squirrel/voice/orchestrator.js` along voice orchestration ownership boundaries. Evidence: kept `orchestrator.js` as the public voice orchestrator facade and moved focused owners into `orchestrator_env.js`, `orchestrator_invocation.js`, `orchestrator_connectors.js`, `orchestrator_bridge.js`, `orchestrator_reply.js`, `orchestrator_mail_summary.js`, `orchestrator_planning_runtime.js`, `orchestrator_agent_toolchain.js`, `orchestrator_tool_router_runtime.js`, and `orchestrator_execution_runtime.js`; removed dead pending-connector/soft-timeout/progress-placeholder helpers; fixed the extracted AtomeAI toolchain confirmation reference; preserved router security by creating durable confirmation metadata only for the explicit `mail:send` follow-up command while auto-send/reply paths still require router confirmation; updated CODEMAP/API_MAP; reduced `orchestrator.js` 2011->382 lines and all orchestrator modules are below 500 lines; `node --check` PASS for the facade and all extracted modules; `node --test atome/src/squirrel/voice/orchestrator.test.mjs atome/src/squirrel/voice/orchestrator.mail_flows.test.mjs atome/src/squirrel/voice/orchestrator.mail_reply.test.mjs atome/src/squirrel/voice/orchestrator.ai_planner.test.mjs atome/src/squirrel/voice/orchestrator.communication_surfaces.test.mjs atome/src/squirrel/voice/orchestrator.contacts_direct_update.test.mjs atome/src/squirrel/voice/orchestrator.contacts_field_reply.test.mjs atome/src/squirrel/voice/orchestrator.runtime_param_resolution.test.mjs atome/src/squirrel/voice/orchestrator.runtime_tool_failure.test.mjs atome/src/squirrel/voice/orchestrator.trace_store.test.mjs` PASS 10/10; `node --test atome/src/squirrel/voice/service.test.mjs` PASS; `npm run check:syntax` PASS 715 files. Progress: 50% complete, 21/42 steps remaining.
- [x] R12 Reduce `atome/src/squirrel/voice/panel.js` without moving product-specific UI into Atome. Evidence: removed the dead `isTauriLikeEnv` duplicate (defined but never called in panel.js; the live copy stays in `voice/bootstrap.js`); split the monolithic `mountVoicePanel` along view/controller/diagnostic boundaries — `panel_view.js` owns the disposable panel DOM composition (`createElement`/`createButton`, all element handles, log appender), `panel_probe.js` owns the interrupt-cancellation diagnostic controller, and `panel.js` keeps the behavior controller wiring panel actions to the voice API; no UI ownership moved into Atome core (all three modules stay under `atome/src/squirrel/voice/`); reduced `panel.js` 713->405 with `panel_view.js` 260 and `panel_probe.js` 115, all below 500; `node --check` PASS for all three; extended `panel.test.mjs` with probe-path coverage (running -> awaiting_command -> validated) and `node --test atome/src/squirrel/voice/panel.test.mjs` PASS; updated `maps/CODEMAP.md` voice main-files ownership; `npm run check:syntax` PASS 695 files. Progress: 42% complete, 24/42 steps remaining.
- [x] R13 Reduce `atome/src/squirrel/voice/service.js` along voice service/runtime ownership boundaries. Evidence: kept `service.js` as the public `createVoiceService` factory and extracted shared support/env helpers to `service_support.js`, locale/hints/correction/voice selection to `service_speech.js`, provider selection and public provider decision exports to `service_providers.js`, Web Speech recognition to `service_browser_stt.js`, Tauri STT lifecycle/recovery/silence handling to `service_tauri_stt.js`, and browser speech synthesis/stop handling to `service_tts_runtime.js`; updated CODEMAP/API_MAP; reduced `service.js` 1422->479 lines and all service modules are below 500 lines; file-by-file `node --check` PASS for the facade and extracted modules; `node --test atome/src/squirrel/voice/service.test.mjs atome/src/squirrel/voice/service.tauri_stt.test.mjs atome/src/squirrel/voice/service.ai_failure_speech.test.mjs` PASS 3/3; `npm run check:syntax` PASS 721 files. Progress: 52% complete, 20/42 steps remaining.
- [x] R14 Reduce `eVe/core/atome_events/drag_runtime.js` along canonical drag-event ownership boundaries. Evidence: completed the prior partial cleanup by keeping `drag_runtime.js` as the canonical bind/move/snap/gesture-frame coordinator; extracted pointerdown arming into `eVe/core/atome_events/drag_arm_runtime.js`, release/cancel/drop cleanup into `eVe/core/atome_events/drag_finish_runtime.js`, per-pointer mutable state/reset into `eVe/core/atome_events/drag_session_state.js`, and preserved deps-independent geometry/shadow helpers in `drag_session_support.js`; updated CODEMAP/API_MAP; reduced `drag_runtime.js` 947->432 lines and all drag runtime modules are below 500 lines; `node --check eVe/core/atome_events/drag_runtime.js eVe/core/atome_events/drag_arm_runtime.js eVe/core/atome_events/drag_finish_runtime.js eVe/core/atome_events/drag_session_state.js eVe/core/atome_events/drag_session_support.js eVe/core/atome_events.js` PASS; `node --test tests/probes/flower_drag_cancel_contract.test.mjs` PASS; `npm run test:run -- tests/eve/project_scene_multi_selection_transform.test.mjs tests/eve/project_scene_gesture_performance.test.mjs tests/eve/project_scene_stale_drag_regression.test.mjs` PASS 19/19; `npm run check:syntax` PASS 733 files. Progress: 64% complete, 15/42 steps remaining.
- [x] R15 Reduce `eVe/domains/media/api/media_api_shared.js` without reintroducing MTrax media naming or fallback media routes. Evidence: kept `media_api_shared.js` as the compatibility export facade; extracted auth/backend/protected streaming query helpers into `media_api_auth.js` and Fastify-to-local availability/sync into `media_api_local_availability.js`; updated maps and preserved `/api/uploads`, `/api/recordings`, and `/api/extract-audio` route boundaries without MTrax aliases or fallback media routes; reduced `media_api_shared.js` 906->116 lines, extracted modules 425/434 lines; `node --check` PASS for all three modules; import smoke test PASS for exported facade functions; `node --test eVe/domains/media/shared/media_source.test.mjs` PASS; `node --test tests/eve/media_source.test.mjs` PASS; `node --test tests/probes/project_render_legacy_audit.test.mjs` PASS 27/27; `npm run check:syntax` PASS 680 files. Progress: 16% complete, 35/42 steps remaining.
- [x] R16 Reduce `eVe/domains/media/asset_box.js` along media asset-box ownership boundaries. Evidence: kept `asset_box.js` as the public upload facade and extracted focused owners into `asset_box_auth.js` (runtime/API base/auth headers), `asset_box_media.js` (file type, dimension probes, SVG-shape detection), `asset_box_paths.js` (path normalization), `asset_box_selection_state.js` (share selection and Atome cache), `asset_box_atome_store.js` (plain uploaded media Atome creation/listing), `asset_box_upload_transport.js` (upload metadata/download/send transport), `asset_box_panel_dom.js` (disposable upload panel/drop-zone DOM), `asset_box_panel_render.js` (uploads/Atomes list rendering), and `asset_box_debug.js`; updated `CODEMAP/API_MAP` and the sanitizer/runtime-detection test ownership; reduced `asset_box.js` 1874->139 lines and all extracted modules are below 500 lines; file-by-file `node --check` PASS for the facade and all extracted modules; public import smoke PASS for `sendFileToServer`/`createUploadAtome`/SVG inference; `node --test tests/probes/svg_upload_shape_contract.test.mjs` PASS 2/2; `npm run test:run -- tests/eve/media_asset_box.sanitization.test.mjs` PASS 2/2; `npm run check:syntax` PASS 733 files. Progress: 57% complete, 18/42 steps remaining.
- [x] R17 Reduce `eVe/domains/media/media_diagnostics.js` after its MTrack diagnostic removal. Evidence: extracted diagnostic URL/rect helpers to `eVe/domains/media/media_diagnostics_url.js` and HTTP/probe-element checks to `eVe/domains/media/media_diagnostics_probe.js`; kept `media_diagnostics.js` as the report/runtime orchestrator and preserved the no-MTrack diagnostic boundary; updated maps; reduced `media_diagnostics.js` 670->327 lines; `node --check` PASS for all three modules; `node --test tests/eve/media_projection_state.dom_contract.test.mjs` PASS; `node --test tests/eve/media_fixture_restore_contract.test.mjs` PASS; `node --test tests/eve/media_atom_integrity.test.mjs` PASS; `npm run check:syntax` PASS 680 files. Progress: 14% complete, 36/42 steps remaining.
- [x] R18 Reduce `eVe/domains/rendering/bevy_projection_adapter.js` while preserving Bevy projection adapter ownership. Evidence: reduced 502->485 lines without behavior changes; `node --check eVe/domains/rendering/bevy_projection_adapter.js` PASS; `npm run test:run -- tests/eve/bevy_projection_adapter_contract.test.mjs` 20/20; `npm run test:run -- tests/eve/bevy_project_renderer_guards.test.mjs` 8/8. Progress: 4% complete, 40/42 steps remaining.
- [x] R19 Reduce `eVe/elements/eVe_look.js` along existing design-token/style factory boundaries. Evidence: kept `eVe_look.js` as the public design facade and split token translation, root CSS variable installation, base style, Goey menu, Matrix, Calendar, utility presets, chrome/communication/control presets, composed preset registry, and tool theme ownership into focused `eVe/elements/look/*` modules; updated `maps/DESIGN_MAP.md`, `maps/CODEMAP.md`, and `maps/API_MAP.md`; reduced `eVe_look.js` 4312->77 lines and all extracted modules are below 500 lines; `node --check` PASS for the facade and extracted look modules; import smoke PASS for public exports and 132 composed presets; `node --test tests/probes/panel_chrome_contract.test.mjs` PASS; `npm run check:syntax` PASS 693 files. Note: `npm run check:palette-ssot` is not executable in the current tree because it reads missing `atome/src/application/examples/tools.js`. Progress: 30% complete, 29/42 steps remaining.
- [x] R20 Reduce `eVe/i18n/languages.js` without breaking the existing `eveT` key ownership. Evidence: kept `languages.js` as the public message registry entry point and split locale data into `languages_fr_core.js`, `languages_fr_account.js`, `languages_en_core.js`, and `languages_en_account.js`; fixed the pre-existing missing English `eve.menu.delete` key so FR/EN parity is 497/497; updated `maps/CODEMAP.md`; reduced `languages.js` 1010->20 lines and all extracted modules are below 500 lines; `node --check` PASS for all five i18n modules; import parity smoke test PASS for locale exports and FR/EN key parity; `npm run check:syntax` PASS 693 files. Progress: 28% complete, 30/42 steps remaining.
- [x] R21 Reduce `eVe/intuition/runtime/group_visual_runtime.js` without expanding legacy non-project DOM rendering. Evidence: extracted disposable non-project group preview DOM ownership to `eVe/intuition/runtime/group_visual_preview_runtime.js` while keeping membership, group-step synchronization, visual refresh orchestration, and public runtime facade in `group_visual_runtime.js`; updated `maps/ARCHITECTURE_MAP.md`, `maps/CODEMAP.md`, `maps/DESIGN_MAP.md`, and `maps/API_MAP.md`; reduced `group_visual_runtime.js` 620->331 lines and new preview runtime is 324 lines; `node --check` PASS for both modules; `node --test tests/eve/group_state_runtime.dom_contract.test.mjs tests/probes/project_render_legacy_source_audit.test.mjs` PASS 12/12; `node --test tests/probes/project_render_legacy_audit.test.mjs tests/probes/project_render_legacy_source_audit.test.mjs tests/probes/project_render_legacy_media_runtime.test.mjs tests/probes/project_render_legacy_sync_runtime.test.mjs` PASS 27/27; `npm run check:syntax` PASS 693 files. Progress: 33% complete, 28/42 steps remaining.
- [x] R22 Reduce `eVe/intuition/runtime/tool_genesis.js` by extracting real non-project runtime owners only. Evidence: completed the earlier extraction by moving pure projection support into `tool_genesis_projection_support_runtime.js`, project/user/shared-override lifecycle and cleanup into `tool_genesis_lifecycle_runtime.js`, host registry/project-index/persistence-diagnostic composition into `tool_genesis_host_lifecycle_runtime.js`, cross-cutting info-panel/mount/mutation/lifecycle setup into `tool_genesis_core_services_runtime.js`, and public `toolBase`/realtime/bootstrap installation into `tool_genesis_public_runtime.js`; removed now-unused facade destructuring; updated `CODEMAP/ARCHITECTURE_MAP/API_MAP` and source guards; reduced `tool_genesis.js` 3113->499 lines while all `tool_genesis_*_runtime.js` modules remain below 500 lines; `node --check` PASS for the facade, new runtimes, and touched probes; `node --test tests/probes/tool_genesis_create_atome_order.test.mjs tests/probes/project_render_legacy_audit.test.mjs tests/probes/project_render_legacy_source_audit.test.mjs tests/probes/project_render_legacy_media_runtime.test.mjs tests/probes/project_render_legacy_sync_runtime.test.mjs tests/probes/svg_upload_shape_contract.test.mjs` PASS 35/35; `npm run test:run -- --testTimeout 30000 tests/eve/imported_media_durability_contract.test.mjs` PASS 1/1; `node --test tests/eve/selection.sanitization.test.mjs tests/server/atome_persistence_boundary.test.mjs` PASS 3/3; `npm run check:syntax` PASS 733 files. Progress: 60% complete, 17/42 steps remaining.
- [x] R23 Reduce `eVe/intuition/tools/capture.js` along capture action/runtime ownership boundaries. Evidence: kept `capture.js` as the capture action/menu/public API facade; extracted source/action resolution into `capture_source_runtime.js`, preview/freeze session ownership into `capture_preview_session_runtime.js`, quick-record pointer/session state into `capture_quick_record_runtime.js`, fullscreen preview lifecycle into `capture_fullscreen_runtime.js`, and fullscreen chrome controls into `capture_fullscreen_chrome_runtime.js`; fixed the capture menu registration so `capture` is added to existing `toolbox.children` and the real `tool.main.capture` button is projected instead of only existing in `new_menu_v2.getContent()`; updated CODEMAP/DESIGN_MAP/API_MAP; reduced `capture.js` 1772->424 lines and all capture runtimes are below 500 lines; `node --check` PASS for the facade, extracted runtimes, and fullscreen probe; `node --test tests/eve/selection.sanitization.test.mjs tests/probes/eve_runtime_coverage.test.mjs tests/probes/matrix_rendering_migration_contract.test.mjs` PASS 6/6; `node --test tests/probes/photo_video_capture_fullscreen_probe.test.mjs` PASS with photo/video project state, Bevy project canvas projection, `legacy_media_count: 0`, and emergency fullscreen close; `npm run check:syntax` PASS 733 files. Progress: 62% complete, 16/42 steps remaining.
- [x] R24 Reduce `eVe/intuition/tools/core/svg_draw_runtime.js` along SVG draw runtime ownership boundaries. Evidence: extracted SVG draw DOM/project-pointer/live-markup helpers into `eVe/intuition/tools/core/svg_draw_dom_runtime.js` and draw gesture id/commit/patch helpers into `eVe/intuition/tools/core/svg_draw_commit_runtime.js`; removed the empty draw diagnostic hook; updated `maps/CODEMAP.md` and `maps/API_MAP.md`; reduced `svg_draw_runtime.js` 730->498 lines and extracted modules are 185/59 lines; `node --check` PASS for all three modules; `node --test eVe/intuition/tools/core/svg_draw_model.test.mjs tests/probes/project_render_legacy_source_audit.test.mjs` PASS 12/12; `node --test tests/probes/project_render_legacy_audit.test.mjs tests/probes/project_render_legacy_source_audit.test.mjs tests/probes/project_render_legacy_media_runtime.test.mjs tests/probes/project_render_legacy_sync_runtime.test.mjs` PASS 27/27; `npm run check:syntax` PASS 693 files. Progress: 35% complete, 27/42 steps remaining.
- [x] R25 Reduce `eVe/intuition/tools/core/svg_vector_edit_runtime.js` along SVG vector edit ownership boundaries. Evidence: removed the dead vector diagnostic subsystem entirely — the no-op `logVectorDiag`, the never-called `pushVectorDiagEntry`/`isVectorDiagEnabled`, the `noteMutationWindow` mutation-storm telemetry with its `vectorState` storm fields and `VECTOR_MUTATION_STORM_*` constants, the `getDiagLog`/`clearDiagLog` `window.__eveVectorTool` API (zero external consumers; the three `__eveVectorTool` callers in `eVeIntuition.js`, `tool_runtime.js`, and the creative probe use only `isActive`/`setActive`/`activate`/`deactivate`), the `__EVE_VECTOR_DEBUG*` flag/log keys, and the now-dead `performance.now()` refresh timing plus `pendingRefreshReason` reason-threading that only fed the removed logs; kept production activation/drag/commit/double-click/`window.__eveVectorTool` install ownership in `svg_vector_edit_runtime.js` and refresh/parse/session/handle-rerender ownership in `svg_vector_refresh_runtime.js`; reduced `svg_vector_edit_runtime.js` 490->384 and `svg_vector_refresh_runtime.js` 162->90; `node --check` PASS for both; import smoke PASS for the full dom/mutation/refresh/model chain plus `createVectorRefreshRuntime().scheduleRefresh()` safe no-op path; `npm run check:syntax` PASS 693 files; maps unchanged (CODEMAP ownership descriptions stay accurate and the diagnostics were never mapped). Progress: 40% complete, 25/42 steps remaining.
- [x] R26 Reduce `eVe/intuition/tools/detail.js` after deleted MTrax/karaoke cleanup. Evidence: kept `detail.js` as the closed detail-panel facade and split real owners into `detail_actions.js` (UI action registration), `detail_state.js` (constants/shared state/record mirror/source normalization), `detail_view.js` (dialog DOM and preview/summary/SVG/take rendering), `detail_context_tools.js` (footer/context tools), `detail_editor_model.js` (pure karaoke line payload helpers), and `detail_editor_runtime.js` (dormant lyrics/karaoke editor interaction/sync); preserved the dormant karaoke UI pending Bevy karaoke host rebuild instead of deleting it; updated CODEMAP/API_MAP; reduced `detail.js` 1915->492 lines and all detail modules are below 500 lines; `node --check` PASS for `detail.js`, `detail_actions.js`, `detail_state.js`, `detail_view.js`, `detail_context_tools.js`, `detail_editor_model.js`, `detail_editor_runtime.js`, `detail_styles.js`, and `detail_styles_karaoke.js`; jsdom import smoke PASS with detail dialog creation and `window.open_detail_panel`/`window.close_detail_panel` installed; `npm run check:syntax` PASS 733 files. Progress: 73% complete, 11/42 steps remaining.
- [x] R27 Reduce `eVe/intuition/tools/finder.js` along Finder tool ownership boundaries. Evidence: removed dead code first — `toErrorMessage`, `isTauriRuntime`, `getCurrentProjectId` (defined, never called), `logFinderToolsSources` (computed nothing observable; its scope-button call removed), and the `api.atomes.get` people-lookup fallback whose `extractAtomeFromGetResult` callee is undefined (always threw → caught → null, so behavior-preserving to drop); then split the 2623-line side-effect module along ownership boundaries using a `finderEls` DOM handle registry: `finder_state.js` (finderState + finderEls + constants), `finder_record_model.js` (pure Atome/user normalization), `finder_record_projection.js` (tool-record classification + query matching + property options + drag payload), `finder_data_sources.js` (API loaders + registry divergence heal), `finder_refresh.js` (record refresh + event bus), `finder_view.js` (filter/sort/header/render), `finder_row.js` (selection/drag/rename), `finder_filters.js` (Filters accordion sub-panel + toggle), and `finder_controller.js` (scope/context + open/close/refresh/quick-search lifecycle + window exposure); kept `finder.js` as the dialog skeleton builder + lazy-import entry. Also removed the dead debug block in `runFilter` (toolsLogSignature feeding an empty forEach). Updated `maps/CODEMAP.md`. Reduced `finder.js` 2623->347 lines and all ten modules are below 500 (state 98, row 173, data_sources 196, controller 241, refresh 262, filters 264, record_model 344, finder 347, view 384, record_projection 385). `node --check` PASS for all ten modules; static import/export cross-reference audit PASS (every sibling import resolves to a real export); runtime import smoke PASS — the nine non-entry modules load with no circular-import/TDZ throw and `normalizeRecord`/`matchCondition`/`buildFinderDragPayload` behave correctly; `npm run check:syntax` PASS 733 files. `finder.js`'s own DOM construction requires the live app (same gating as M7.1/M7.2), so node --check + cross-reference audit + sibling import smoke is the narrowest available structural validation. Progress: 71% complete, 12/42 steps remaining.
- [x] R28 Reduce `eVe/intuition/tools/layer.js` after MTrack entanglement removal. Evidence: confirmed the file already carries zero MTrack/MTrax/clip_id/eveMtrackApi references (entanglement removal complete), so this step is the size reduction; split along view/engine/style boundaries — `eVe/intuition/tools/core/svg_layer_store.js` (398) owns the canonical per-Atome project layer-selection store, SVG layer markup add/remove/update mutation, the `eve:svg-layer-*` emitters, and the `createProjectSvgLayerApi` command factory; `eVe/intuition/tools/layer_panel_styles.js` (105) owns the injected panel CSS; `layer.js` keeps the panel DOM, row rendering, refresh, `ui.layer.select`, and `window.eveSvgLayerApi` installation; reduced `layer.js` 825->358, all three below 500; `node --check` PASS for all three; static import-resolution check PASS (all 18 names imported by `layer.js` exist as real exports of the store/styles/model modules); store API smoke PASS (`createProjectSvgLayerApi` exposes the 7 expected methods with correct empty-input error contracts); `layer.js` still satisfies the simple-panel contract in isolation (`createEveDialog({` + `id: 'eve_layer_dialog'` present); `npm run check:syntax` PASS 695 files; updated `maps/CODEMAP.md` layer ownership. Note: `tests/probes/simple_panel_migration_contract.test.mjs` is red on a pre-existing, unrelated failure — `delete.js` lost `createEveDialog({` in its prior panel_view.js split (HEAD still had it) and `delete` is iterated before `layer`; flagged for separate fix. Progress: 45% complete, 23/42 steps remaining.
- [x] R29 Reduce `eVe/intuition/tools/perform.js` along perform-tool ownership boundaries. Evidence: removed the dead `window.evePerformDebug` test surface (`clickMainTool`/`invokeGateway`/`invokeRuntime`/`invokeRegisteredHandler` had zero consumers; its only used method `snapshot` is duplicated by `evePerformApi.snapshot`), removed the no-op `logPerformDebug` and its 16 call sites plus the now-dead `extractDebugErrorText`, and removed the unused `resolvePrimarySelectedAtomeId`/`toEscapedSelectorValue` dead helpers; also removed the redundant `window.evePerformDebug?.snapshot()` active-check in `eVeIntuition.js:7035` (identical to the `evePerformApi.isActive()` line above it); kept `perform.js` as the lifecycle/pointer-gating/tool-registration/`window.evePerformApi` facade and split cohesive owners into `perform_state.js` (constants + `performState`/`preferenceState` singletons), `perform_support.js` (stateless DOM helpers), `perform_menu_runtime.js` (toolbox preview reveal), `perform_focus_runtime.js` (system-UI hide + Atome isolation), and `perform_preference_runtime.js` (profile persistence via injected isActive/deactivate hooks to avoid a circular import); updated `maps/CODEMAP.md`; reduced `perform.js` 1366->403 lines and all extracted modules are below 500 (state 94, support 241, preference 163, focus 203, menu 169); `node --check` PASS for the six perform modules and `eVeIntuition.js`; import smoke test (DOM/window stub) PASS — module loads with no throw and `window.evePerformApi` exposes all six methods with `isActive()` false at rest; `npm run check:syntax` PASS 733 files. Progress: 66% complete, 14/42 steps remaining.
- [x] R30 Reduce `eVe/intuition/tools/selection_style_apply.js` along selection-style ownership boundaries. Evidence: removed the unreachable atomic inline text-selection range-mutation subsystem gated by the compile-time `const ATOMIC_INLINE_TEXT_SELECTION_MUTATIONS_ENABLED = false` (the flag is referenced only in this file and never toggled, so the whole `applyInlineStyleToActiveTextSelection` body and its exclusively-used helpers — `applyInlineStyleToTextNodeSerializedRanges`, `resolveReusableInlineStyleTarget`, `stripInlineStylesForPatch`, `doesRangeMatchNodeContents`, `RANGE_START_TO_START`/`RANGE_END_TO_END`, `rebindRememberedTextSelection`, `restoreSerializedSelectionOnTextNode` — were unreachable); replaced it with a behavior-preserving `applyInlineStyleToActiveTextSelection` stub returning the same `atomic_inline_text_selection_unsupported` result the three callers already always received, and dropped the now-dead `escapeAttrValue` and `fitTextHostToContent` import; then split the remaining file along ownership boundaries into a clean import DAG: `selection_style_state.js` (constants + memory/intent singletons + pure state predicates), `selection_style_ranges.js` (stateless range/number/font-size helpers), `selection_style_context.js` (selection target resolvers), `selection_style_text_selection.js` (live/remembered text-range contexts + current size), `selection_style_memory.js` (capture/restore + selectionchange bridge), `selection_style_atome.js` (Atome state/property/mutation), and `selection_style_svg_layer.js` (SVG layer color), keeping `selection_style_apply.js` as the public couleur/size/font facade; updated `maps/CODEMAP.md`; reduced `selection_style_apply.js` 1737->252 lines and all seven extracted modules are below 500 (state 114, ranges 123, svg_layer 184, atome 191, context 200, memory 207, text_selection 240); `node --check` PASS for all eight modules; import smoke test (browser-global stub) PASS — full transitive chain loads with no circular-import/TDZ throw, all six exports present and callable, and at-rest behavior preserved (readSelectionCount 0, resolveCurrentTextSizeValue 96, hasTextStyleRangeSelectionIntent false); `npm run check:syntax` PASS 733 files. The Playwright/live-app text-styling scenarios remain gated by the same live-app requirement as M7.1/M7.2 so `node --check` + import smoke is the narrowest available structural validation. Progress: 69% complete, 13/42 steps remaining.
- [x] R31 Reduce `eVe/intuition/tools/user.js` after MTrax user-preference removal. Evidence: completed the prior extraction by keeping `user.js` as a 499-line facade and moving real owners into `user_visual_preferences_runtime.js`, `user_identity_fields_runtime.js`, `user_profile_sections_runtime.js`, `user_background_language_preferences.js`, `user_display_runtime.js`, `user_panel_mode_runtime.js`, `user_action_buttons_runtime.js`, `user_home_panel_runtime.js`, `user_profile_lifecycle_runtime.js`, `user_auth_flow_runtime.js`, `user_preferences_cache_runtime.js`, `user_dialogs_runtime.js`, and `user_panel_reset_runtime.js`; preserved Bevy/WebGPU rendering boundaries by touching only user-panel UI/profile orchestration; updated CODEMAP/DESIGN_MAP; all extracted user modules stay below 500 lines; `node --check` PASS for `user.js` and extracted user runtimes; `node --test tests/probes/user_panel_content_contract.test.mjs tests/probes/background_quick_action_contract.test.mjs tests/probes/svg_upload_shape_contract.test.mjs` PASS 5/5; `npm run check:syntax` PASS 733 files. Progress: 76% complete, 10/42 steps remaining.
- [x] R32 Reduce `atome/src/squirrel/ai/default_tools.runtime_bridge.test.mjs` without weakening bridge coverage. Evidence: extracted only the environment/service fixture into `atome/src/squirrel/ai/default_tools.runtime_bridge.fixture.mjs`; kept bridge assertions in the original test; reduced `default_tools.runtime_bridge.test.mjs` 558->302 lines and fixture is 254 lines; `node --check` PASS for both files; `node --test atome/src/squirrel/ai/default_tools.runtime_bridge.test.mjs` PASS; `node --test atome/src/squirrel/ai/default_tools.runtime_trace_integration.test.mjs` PASS; `npm run check:syntax` PASS 681 files. Progress: 19% complete, 34/42 steps remaining.
- [x] R33 Reduce `atome/src/squirrel/voice/orchestrator.test.mjs` without weakening voice orchestration coverage. Evidence: split the monolithic voice orchestrator test into core runtime/MCP/session/journal coverage in `orchestrator.test.mjs`, mail read/status/summary/filter flows in `orchestrator.mail_flows.test.mjs`, mail reply confirmation/context flows in `orchestrator.mail_reply.test.mjs`, and reusable planner/env fixtures in `orchestrator.planner_fixture.mjs` plus `orchestrator.test_fixture.mjs`; updated `maps/CODEMAP.md`; reduced `orchestrator.test.mjs` 1196->177 lines and all new files are below 500 lines; `node --check` PASS for all five touched modules; `node --test atome/src/squirrel/voice/orchestrator.test.mjs atome/src/squirrel/voice/orchestrator.mail_flows.test.mjs atome/src/squirrel/voice/orchestrator.mail_reply.test.mjs` PASS 3/3; `npm run check:syntax` PASS 686 files. Progress: 23% complete, 32/42 steps remaining.
- [x] R34 Reduce `tests/eve/bevy_projection_adapter_contract.test.mjs` without weakening Bevy adapter guardrails. Evidence: moved filter/transition effect guardrails into `tests/eve/bevy_projection_adapter_effects.test.mjs`; kept core adapter contract coverage in `tests/eve/bevy_projection_adapter_contract.test.mjs`; reduced contract test 610->448 lines and new effects test is 168 lines; `node --check` PASS for both tests; `npm run test:run -- tests/eve/bevy_projection_adapter_contract.test.mjs tests/eve/bevy_projection_adapter_effects.test.mjs` PASS 14/14; `npm run test:run -- tests/eve/bevy_project_renderer_guards.test.mjs` PASS 8/8; `npm run check:syntax` PASS 682 files. Progress: 21% complete, 33/42 steps remaining.
- [x] R35 Reduce `tests/probes/browser_media_acceptance_probe.test.mjs` without weakening browser media acceptance coverage. Evidence: the probe is a 165-line orchestrator delegating to extracted `browser_media_acceptance_probe_runtime.mjs` (286), `browser_media_acceptance_probe_inventory.mjs` (163), and `browser_media_acceptance_probe_desktop.mjs` (454); all four modules below 500 lines and import-coupled so desktop acceptance coverage (login, bootstrap import, media import, inventory readiness, per-case desktop verification, summary) is preserved; `node --check` PASS for all four; the Playwright run requires a live `:3001` app (same gating as M7.1/M7.2) so `node --check` is the narrowest available structural validation; `npm run check:syntax` PASS 693 files. Progress: 38% complete, 26/42 steps remaining.
- [x] R36 Reduce `tests/probes/project_render_legacy_audit.test.mjs` without weakening legacy renderer deletion coverage. Evidence: split source-delegation guardrails into `project_render_legacy_source_audit.test.mjs`, media runtime guardrails into `project_render_legacy_media_runtime.test.mjs`, sync/runtime guardrails into `project_render_legacy_sync_runtime.test.mjs`, and shared source loading into `project_render_legacy_audit_fixture.mjs`; kept implicit gesture commit and project-atome index coverage in `project_render_legacy_audit.test.mjs`; updated `maps/CODEMAP.md`; reduced the original test 788->121 lines and all new files are below 500 lines; `node --check` PASS for all five touched files; `node --test tests/probes/project_render_legacy_audit.test.mjs tests/probes/project_render_legacy_source_audit.test.mjs tests/probes/project_render_legacy_media_runtime.test.mjs tests/probes/project_render_legacy_sync_runtime.test.mjs` PASS 27/27; `npm run check:syntax` PASS 693 files. Progress: 26% complete, 31/42 steps remaining.
- [x] R37 Re-audit touched files after reductions and remove any completed P0 entry whose file is now below threshold or has a validated split owner. Evidence: rechecked the completed user-panel reduction set and all `eVe/intuition/tools/user*.js` files are now below 500 lines after simplifying `user_login_credentials.js` 500->496; `node --check eVe/intuition/tools/user_login_credentials.js` PASS; confirmed the remaining oversized active cleanup files are the still-open R1-R5 entries (`eVeIntuition.js`, `tool_runtime.js`, `project_drop.js`, `toolbox_runtime.js`, and `ribbon/menu.js`), so no completed P0 item remains falsely open. Progress: 79% complete, 9/42 steps remaining.
- [x] R38 Run focused Bevy renderer guardrails after P0 reductions. Evidence: `cargo test --manifest-path atome/renderers/bevy-core/Cargo.toml` PASS 19/19; `npm run test:run -- tests/eve/bevy_projection_adapter_contract.test.mjs tests/eve/bevy_projection_adapter_effects.test.mjs tests/eve/bevy_project_renderer_guards.test.mjs` PASS 22/22; `node --test tests/probes/project_render_legacy_audit.test.mjs tests/probes/project_render_legacy_source_audit.test.mjs tests/probes/project_render_legacy_media_runtime.test.mjs tests/probes/project_render_legacy_sync_runtime.test.mjs` PASS 28/28. Progress: 81% complete, 8/42 steps remaining.
- [x] R39 Run `npm run check:syntax` after P0 reductions. Evidence: `npm run check:syntax` PASS 733 files after R31/R37/R38 reductions, Bevy guardrails, maps, and tracker updates. Progress: 83% complete, 7/42 steps remaining.
- [ ] R40 Validate Web M7.1 on `:3001`: montage compositing, filters, transitions, selection, scrub, and A/V sync on the project canvas. Gated by NewMolecules editor reintegration.
- [ ] R41 Validate Tauri/iOS/AUv3 M7.2: same scenario with Kira A/V sync on a looping montage. Gated by NewMolecules editor reintegration.

## Progress Log

- R0 completed. Remaining: R1-R41. Progress: 2% complete, 98% remaining.
- R18 completed. Remaining: R1-R17 and R19-R41. Progress: 4% complete, 96% remaining.
- R6 completed. Remaining: R1-R5 and R7-R17 and R19-R41. Progress: 7% complete, 93% remaining.
- R7 completed. Remaining: R1-R5 and R8-R17 and R19-R41. Progress: 9% complete, 91% remaining.
- R8 completed. Remaining: R1-R5 and R9-R17 and R19-R41. Progress: 11% complete, 89% remaining.
- R17 completed. Remaining: R1-R5, R9-R16, and R19-R41. Progress: 14% complete, 86% remaining.
- R15 completed. Remaining: R1-R5, R9-R14, R16, and R19-R41. Progress: 16% complete, 84% remaining.
- R32 completed. Remaining: R1-R5, R9-R14, R16, R19-R31, and R33-R41. Progress: 19% complete, 81% remaining.
- R34 completed. Remaining: R1-R5, R9-R14, R16, R19-R31, R33, and R35-R41. Progress: 21% complete, 79% remaining.
- R33 completed. Remaining: R1-R5, R9-R14, R16, R19-R31, and R35-R41. Progress: 23% complete, 77% remaining.
- R36 completed. Remaining: R1-R5, R9-R14, R16, R19-R31, R35, and R37-R41. Progress: 26% complete, 74% remaining.
- R20 completed. Remaining: R1-R5, R9-R14, R16, R19, R21-R31, R35, and R37-R41. Progress: 28% complete, 72% remaining.
- R19 completed. Remaining: R1-R5, R9-R14, R16, R21-R31, R35, and R37-R41. Progress: 30% complete, 70% remaining.
- R21 completed. Remaining: R1-R5, R9-R14, R16, R22-R31, R35, and R37-R41. Progress: 33% complete, 67% remaining.
- R24 completed. Remaining: R1-R5, R9-R14, R16, R22-R23, R25-R31, R35, and R37-R41. Progress: 35% complete, 65% remaining.
- R35 completed. Remaining: R1-R5, R9-R14, R16, R22-R23, R25-R31, and R37-R41. Progress: 38% complete, 62% remaining.
- R25 completed. Remaining: R1-R5, R9-R14, R16, R22-R23, R26-R31, and R37-R41. Progress: 40% complete, 60% remaining.
- R12 completed. Remaining: R1-R5, R9-R11, R13-R14, R16, R22-R23, R26-R31, and R37-R41. Progress: 42% complete, 58% remaining.
- R28 completed. Remaining: R1-R5, R9-R11, R13-R14, R16, R22-R23, R26-R27, R29-R31, and R37-R41. Progress: 45% complete, 55% remaining.
- R10 completed. Remaining: R1-R5, R9, R11, R13-R14, R16, R22-R23, R26-R27, R29-R31, and R37-R41. Progress: 47% complete, 53% remaining.
- R11 completed. Remaining: R1-R5, R9, R13-R14, R16, R22-R23, R26-R27, R29-R31, and R37-R41. Progress: 50% complete, 50% remaining.
- R13 completed. Remaining: R1-R5, R9, R14, R16, R22-R23, R26-R27, R29-R31, and R37-R41. Progress: 52% complete, 48% remaining.
- R9 completed. Remaining: R1-R5, R14, R16, R22-R23, R26-R27, R29-R31, and R37-R41. Progress: 54% complete, 46% remaining.
- R16 completed. Remaining: R1-R5, R14, R22-R23, R26-R27, R29-R31, and R37-R41. Progress: 57% complete, 43% remaining.
- R23 completed. Remaining: R1-R5, R14, R26-R27, R29-R31, and R37-R41. Progress: 62% complete, 38% remaining.
- R14 completed. Remaining: R1-R5, R26-R27, R29-R31, and R37-R41. Progress: 64% complete, 36% remaining.
- R29 completed. Remaining: R1-R5, R26-R27, R30-R31, and R37-R41. Progress: 66% complete, 34% remaining.
- R30 completed. Remaining: R1-R5, R26-R27, R31, and R37-R41. Progress: 69% complete, 31% remaining.
- R27 completed. Remaining: R1-R5, R26, R31, and R37-R41. Progress: 71% complete, 29% remaining.
- R26 completed. Remaining: R1-R5, R31, and R37-R41. Progress: 73% complete, 27% remaining.
- R31 completed. Remaining: R1-R5 and R37-R41. Progress: 76% complete, 24% remaining.
- R37 completed. Remaining: R1-R5 and R38-R41. Progress: 79% complete, 21% remaining.
- R38 completed. Remaining: R1-R5 and R39-R41. Progress: 81% complete, 19% remaining.
- R39 completed. Remaining: R1-R5 and R40-R41. Progress: 83% complete, 17% remaining.
- R4 completed. Remaining: R1-R3, R5, and R40-R41. Progress: 86% complete, 14% remaining.
- R5 completed. Remaining: R1-R3 and R40-R41. Progress: 88% complete, 12% remaining.
- R1 completed. Remaining: R2-R3 and R40-R41. Progress: 90% complete, 10% remaining.
- R2 completed. Remaining: R3 and R40-R41. Progress: 92% complete, 8% remaining.
- R3 completed. Remaining: R40-R41. Progress: 95% complete, 5% remaining.
- R40/R41 gate audit. Remaining: R40-R41. Progress: 95% complete, 5% remaining. Evidence: `todo/NewMolecules.md` still has open reintegration/validation items, including V2.4 and the Web/Tauri/iOS/AUv3 parity validation line, so M7.1/M7.2 cannot be truthfully checked yet; maintained Molecule base remains green with `npm run test:molecule` PASS 3 suites and `npm run check:molecule-guardrails` PASS 19 files.
- R40/R41 prerequisite cleanup. Remaining: R40-R41. Progress: 95% complete, 5% remaining. Evidence: removed permanent Molecule group-runtime project diagnostics and global trace buffering from `eVe/intuition/tools/molecule/runtime.js` without adding fallback/API/rendering paths; `node --check eVe/intuition/tools/molecule/runtime.js` PASS; `npm run test:molecule` PASS 3 suites; `npm run check:molecule-guardrails` PASS 19 files; `npm run check:syntax` PASS 733 files. R40/R41 remain open because the executable NewMolecules editor scenario is still gated by open V1.2-V1.9/M7 work in `todo/NewMolecules.md`.
- R40/R41 NewMolecules V1.2 prerequisite completed. Remaining: R40-R41. Progress: 95% complete, 5% remaining. Evidence: added the pure Molecule track-type registry owner `eVe/intuition/tools/molecule/track_types/index.js`, routed `kernel/schemas.js` and `kernel/reducers.js` through it, added maintained coverage in `tests/probes/molecule_track_type_registry.test.mjs`, and updated `tests/molecule/run_molecule_tests.mjs`, `todo/NewMolecules.md`, `maps/CODEMAP.md`, and `maps/API_MAP.md`; `node --test tests/probes/molecule_track_type_registry.test.mjs` PASS 3/3; `npm run test:molecule` PASS 4 suites; `npm run check:molecule-guardrails` PASS 20 files; `npm run check:no-fallbacks` PASS 20 files; `npm run check:syntax` PASS 734 files. R40/R41 stay open until the remaining NewMolecules V1.3-V1.9/M7 executable scenario is complete.
- R40/R41 NewMolecules V1.3 prerequisite completed. Remaining: R40-R41. Progress: 95% complete, 5% remaining. Evidence: added pure Molecule tempo-map and dual-time owner `eVe/intuition/tools/molecule/kernel/time_model.js`, wired `kernel/schemas.js`, `kernel/reducers.js`, and `persistence/index.js` so generated/migrated timeline snapshots carry `timebase.tempo_map` plus transport/clip/marker seconds+musical references, added maintained coverage in `tests/probes/molecule_dual_time_model.test.mjs`, and updated `tests/molecule/run_molecule_tests.mjs`, `todo/NewMolecules.md`, `maps/CODEMAP.md`, and `maps/API_MAP.md`; `node --test tests/probes/molecule_dual_time_model.test.mjs` PASS 4/4; `npm run test:molecule` PASS 5 suites; `npm run check:molecule-guardrails` PASS 21 files; `npm run check:no-fallbacks` PASS 21 files; `npm run check:syntax` PASS 735 files. R40/R41 stay open until the remaining NewMolecules V1.4-V1.9/M7 executable scenario is complete.
- R40/R41 NewMolecules V1.4 prerequisite completed. Remaining: R40-R41. Progress: 95% complete, 5% remaining. Evidence: added Molecule session timeline verb aliasing and session-local clipboard operations in `eVe/intuition/tools/molecule/session/timeline_operations.js` + `session.js`, exposed active group timeline read/single-operation/batch APIs through `window.eveMoleculeTimelineApi`, registered canonical runtime tools in `eVe/intuition/tools/timeline_actions.js`, registered AI tools in `atome/src/squirrel/ai/default_tools_timeline.js`, and gated timeline tools in `atome/src/squirrel/atome/mcp_security_policy.js`; `node --test tests/probes/molecule_session_history.test.mjs atome/src/squirrel/ai/default_tools_timeline.test.mjs atome/src/squirrel/atome/mcp.timeline_policy.test.mjs` PASS 7/7; `node --test atome/src/squirrel/atome/mcp.security_surface.test.mjs atome/src/squirrel/atome/mcp.platform_surface.test.mjs` PASS 2/2; `npm run test:molecule` PASS 5 suites; `npm run check:molecule-guardrails` PASS 22 files; `npm run check:no-fallbacks` PASS 22 files; `npm run check:syntax` PASS 738 files. R40/R41 stay open until the remaining NewMolecules V1.5-V1.9/M7 executable scenario is complete.
- R40/R41 NewMolecules V1.5 render foundation completed (V1.5 in progress). Remaining: R40-R41. Progress: 95% complete, 5% remaining. Evidence: added the pure timeline-to-virtual-scene projection owner `eVe/intuition/tools/molecule/render/timeline_scene.js` mapping a timeline snapshot to flat render records (track lanes, clip rectangles, single playhead) on the dedicated `molecule` layer through the canonical `eVe/domains/rendering/virtual_scene_contract.js` seam — no renderer of its own, seconds drive x-layout (Kira clock, D5), per-clip filter/transition flow to the M1/M2 material path only when present; registered `tests/probes/molecule_timeline_scene.test.mjs` in `tests/molecule/run_molecule_tests.mjs` and updated `maps/CODEMAP.md` + `todo/NewMolecules.md` V1.5. `node --test tests/probes/molecule_timeline_scene.test.mjs` PASS 3/3 (geometry+layer, filter/transition pass-through, scrub = single `updateTransform` op on the playhead via `diffVirtualSceneTrees`); `npm run test:molecule` PASS 6 suites; `npm run check:molecule-guardrails` PASS 23 files; `npm run check:no-fallbacks` PASS 23 files; `npm run check:syntax` PASS 739 files. R40/R41 stay open: V1.5 still needs the molecule runtime wired to emit this scene through the project Bevy adapter and a real `#eve_surface_project` GPU validation, then V1.6-V1.9/M7.
- Boot-break fix (discovered by the product owner running the app): `eVe/intuition/tools/project_drop.js` threw a module-evaluation TDZ (`Cannot access 'resolveFinderToolId'… before initialization`) at app boot because the top-level `createToolDropDedupeRuntime({…})` call (~line 4591) captured `resolveFinderSourceAtomeId`, a `const` not defined until ~line 5326 (latent regression from the R3 split; `node --check`/import smoke validate syntax, not evaluation order). Moved the factory instantiation below all three resolver consts; validated on the real app via `temp/boot_probe.mjs` → BOOT VERDICT PASS (app boots, `#eve_surface_project` present with role `project-webgpu-render-surface`, molecule API wired, 0 console errors). No map change (internal reordering, no ownership/API change).
- R40/R41 NewMolecules V1.5 render path COMPLETED on the real Bevy canvas. Remaining: R40-R41. Progress: 95% complete, 5% remaining. Evidence: added `eVe/intuition/runtime/molecule_timeline_scene_bridge.js` (timeline snapshot → records → `#eve_surface_project` via the new batch `project_scene_runtime.js` `updateProjectSceneRecords`, stale-record reconciliation + clear), exposed `window.eveMoleculeTimelineApi.renderTimelineScene`/`clearTimelineScene`, and made clip blocks `shape` records (carrying `clip_kind`) so `recordsForBevyProjection` cannot drop source-less clips. Real-app probe `temp/molecule_timeline_render_probe.mjs` (visible Chromium/WebGPU): rendered a multitrack timeline on the Bevy canvas (1 lane + 3 clips + playhead, all `molecule` layer, present in project scene state), scrub moved the playhead 0→240px (screenshots `temp/molecule_timeline_render_playhead0.png`/`_playhead3.png`), clear removed all 5 records, 0 console errors. `tests/probes/molecule_timeline_scene_bridge.test.mjs` added + registered in the molecule runner; `npm run test:molecule` PASS 7 suites; Bevy/project-scene contracts `tests/eve/bevy_projection_adapter_contract.test.mjs` + `bevy_project_renderer_guards.test.mjs` 22/22; `check:molecule-guardrails` 23, `check:no-fallbacks` 23, `check:syntax` 740; updated `maps/CODEMAP.md` + `maps/API_MAP.md` + `todo/NewMolecules.md` (V1.5 [x]). R40/R41 stay open until V1.6-V1.9/M7 (selection/transform, automation, Time Machine, full API+MCP+AI edit-session validation) complete.
- R40/R41 NewMolecules V1.6 render side (selection + hit-testing) COMPLETED on the real Bevy canvas. Remaining: R40-R41. Progress: 95% complete, 5% remaining. Evidence: clip blocks are first-class project-scene records, so the existing project-scene selection overlay + hit-testing apply to them with no new code — clips emit `selectable:true`, lanes/playhead `selectable:false`. Real-app probe `temp/molecule_timeline_selection_probe.mjs` (visible Chromium/WebGPU): clip selectable true, lane/playhead false, selecting a clip set `visual.selected:true`, and `temp/molecule_timeline_selection.png` shows the dashed selection rectangle drawn on the selected clip; 0 console errors. **Architectural boundary reached:** session-backed editing (V1.6 drag→`clip.move`/`clip.trim`, V1.7 automation, V1.8 undo/redo) is blocked because the app provides no `moleculeStores` (projectStore/eventStore) — `openGroupTimeline` throws `STORES_REQUIRED`. The next foundational step is wiring a real session/store layer (Time Machine `eventStore.append`, snapshot `projectStore.saveTimeline`), which carries an open product decision on how Molecule timelines persist within the Atome model (documented in `todo/NewMolecules.md` V1.8). V1.5 render + V1.6 selection were intentionally delivered on the pure render path to make real progress without that layer.
- R40/R41 NewMolecules V1.8 store foundation + V1.6 edit path COMPLETED on the real app (product decision: canonical persistence in the Atome). Remaining: R40-R41. Progress: 95% complete, 5% remaining. Evidence: added `eVe/intuition/runtime/molecule_stores.js` — `projectStore.saveTimeline` writes the timeline snapshot as the owner group atome's `molecule_timeline` prop via `window.Atome.commit` (one deterministic Time Machine entry per edit), `loadTimeline` reads via `getStateCurrent`, `eventStore.append` emits on eVe's deterministic bus; installed at boot in `tool_genesis_bootstrap_runtime.js`. `molecule/runtime.js` `openGroupTimeline` now runs the real session, persists, and renders on the Bevy canvas, re-rendering on every committed edit via `onStateCommitted`, and no longer opens the DOM editor panel (canvas is the mandated render path; `molecule/panel/` parked under §7). Real-app probe `temp/molecule_timeline_session_edit_probe.mjs`: open persists+renders, `clip.move` rejected an overlapping target via real collision detection and accepted a free slot, the clip re-rendered on the canvas (0→960px, `temp/molecule_session_after_move.png`), the edit emitted `molecule_timeline` set events into eVe's deterministic log (`setEventCount:6`, persisted `start:12`), close cleared the overlay; 0 console errors. `tests/probes/molecule_stores.test.mjs` added (5/5) + registered; `npm run test:molecule` PASS 8 suites; boot probe PASS; Bevy/project-scene contracts 22/22; `check:molecule-guardrails` 23, `check:no-fallbacks` 23, `check:syntax` 741; updated `maps/CODEMAP.md` + `maps/API_MAP.md` + `todo/NewMolecules.md` (V1.6 edit path, V1.8 [~]). Also added `undoGroupTimeline`/`redoGroupTimeline` to the group API (delegate to the session, re-persist + re-render via `onStateCommitted`); validated live in the same probe — undo returned the clip to 0px, redo back to 960px on the canvas, deterministic. Remaining: V1.6 on-canvas pointer-drag→op + selection_overlay.rs, V1.7 automation, V1.8 MCP/AI undo-redo parity + live batch single-undo, V1.9 full API+MCP+AI edit-session validation, then R40/R41.
