SVG helpers

- `svg_utils.js`: core SVG rendering helpers (`render_svg`, `fetch_and_render_svg`, `sanitizeSVG`).
- `loader.js`: still exposes `render_svg`/`fetch_and_render_svg` on `window` for compatibility.
