/* WaveSurfer has been intentionally disabled in this build. */
(function (global) {
    function throwDisabledError() {
        throw new Error('WaveSurfer is disabled in this build.');
    }

    global.WaveSurfer = {
        create: throwDisabledError,
    };
})(globalThis);
